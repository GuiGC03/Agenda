<h1>Todas as Agendas:</h1>
<ul class="agenda-list">
  {{#if emptyAgendas}}
    <p>Não há agendas cadastradas</p>
  {{/if}}
  {{#each agendas}}
    <li>
      {{#if this.done}}
        <a href="/agendas/edit/{{this.id}}" class="titulo done">{{this.titulo}}</a>
      {{else}}
        <a href="/agendas/edit/{{this.id}}" class="titulo">{{this.titulo}}</a>
      {{/if}}
      <span class="actions">
        <form action="/agendas/updatestatus" method="POST">
          <input type="hidden" name="id" value={{this.id}}>
          <input type="hidden" name="done" value={{this.done}}>
          <button type="submit">
            <i class="bi bi-check2"></i>
          </button>
        </form>
        <a href="/agendas/edit/{{this.id}}"><i class="bi bi-pencil-square"></i></a>
        <form action="/agendas/remove" method="POST">
          <input type="hidden" name="id" value={{this.id}}>
          <button type="submit">
            <i class="bi bi-x-lg"></i>
          </button>
        </form>
      </span>
    </li>
  {{/each}}
</ul>